1.数据持久化：将那些内存中的瞬时数据保存到存储设备中，保证即使在设备关机的情况下，这些数据也不会丢失

2.主要的3种方式
    1.文件存储
    2.sharedPreference存储
    3.数据库存储

3.文件存储：最基本的一种存储方式
          将数据原封不动的存储下来
          适用于存储一些简单的文本数据和二进制数据

          所有文件都默认存储到/data/data/包名/files目录下

          this.openFileOutput("file", MODE_PRIVATE);
          第一个参数表示文件名
          第二个参数表示文件的操作模式：MODE_PRIVATE: 同样文件名的时候，所写入的内容会覆盖源文件的内容
                                   MODE_APPEND: 文件已经存在时，继续向里面的写内容会
                                   MODE_WORLD_READABLE和MODE_WORLD_WRITEABLE :允许其他应用程序队我们程序中的文件进行读写操作

4.SharedPreferences存储
    健值对方式存储
    获取sharedPreferences对象获取方法
        1.context.getSharedPreferences("data",Context.MODE_PRIVATE); 第一个参数是指定sharedPreference的文件名
                                                                     第二个参数是操作模式（只有一种MODE_PRIVATE：表示只有当前的应用程序才可以对这个文件进行读写）
        2.PreferenceManager.getDefaultSharedPreferences(this);静态方法，只接收一个参数Context，并自动使用当前应用程序的包名作为前缀来命名SharePreference文件

5.内容提供者（contentProvider）：用于不同程序之间实现数据共享的功能，并且确保了数据的安全性

6.权限可以分为2类
    普通权限：系统会自动授权，不会直接威胁到用户的安全和隐私的权限
    危险权限：可能会触及用户隐私，或者对设备安全性造成影响的权限。如：获取设备联系人信息灯  对于这类权限，需要用户手动授权 具体可见img_6.png