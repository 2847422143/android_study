1.Android 是使用任务（Task）来管理活动的,一个任务就是一组存放在栈中的活动的集合，这个栈也可以称为返回栈（Back Stack）
    栈是一种后进先出的数据结构
    系统总是会显示处于栈顶的活动给用户

2.活动状态：
    运行状态：位于栈顶，且完全可见
    暂停状态：不位于栈顶，但是仍存在部分可见，比如小窗口后面的大窗口（内存极低的情况下，系统才会回收这个活动）
    停止状态：不位于栈顶，且完全不可见（当其他地方需要内存的时候，该活动可能会被系统回收）
    销毁状态：活动从栈中移除之后的状态，（系统最倾向回收的活动）

3.活动生命周期
    onCreate：活动第一次创建的时候调用，用于做初始化操作
    onStart：活动由不可见变成可见的时候调用
    onResume：活动已经准备好和用户交互的时候调用，这个时候就处于运行状态
    onPause：当系统准备启动或恢复另一个活动的时候调用（通常做一些消耗CPU资源的释放和保存数据）
    onStop：活动完全不可见的时候调用，这个时候位于停止状态
    onDestroy：活动被销毁之前调用，之后活动状态就变成了销毁状态
    onRestart：活动由停止状态变成运行状态的时候调用
    详细可见 img_3

3.如果活动被回收了，那重新打开它的声明周期就要从onCreate开始，而不是从onRestart开始

4.如果要在回收前保存数据，可以重写onSaveInstanceState方法

5.启动模式
    standard 默认模式
    每次启动一个活动都会创建实例，并放入栈中，比如 a 启动 a，这样栈中就会有2个a的实例
    singleTop 栈顶复用 （通知栏，连续点多条通知不会创建多个页面，信息页）
    如果启动的活动已经是在栈顶了，就不会创建新的实例
    singleTask 栈内复用  （主页，回到主页后，按返回直接退出 APP，不会退中间页面。）
    当启动一个活动时，如果栈内存在该活动的实例，直接只用该实例，并将处于它前面的活动全部出栈（处于销毁状态），将他位于栈顶
    singleInstance  独占栈 （系统拨号界面 微信支付页面 支付宝支付页 地图导航页面）
    使用这个模式，这个活动独享一个返回栈，这个表示能和其他程序共享这个栈

6.控件的可见行设置
    android：visibility 可选值
    visible 默认状态 可见的
    invisible 控件不可见，但是仍然保留原来的位置和大小
    gone 控件不可见，且不占屏幕位置

7.线性布局LinearLayout  相对布局RelativeLayout  帧布局FrameLayout  百分比布局

8.自定义控件 可以看下控件的基本继承逻辑 img_4
    可以看到所有的控件和布局都直接或间接的继承自View
    View是Android中最基本的一种UI组件，它可以在屏幕上绘制一块矩形区域，并能响应这块区域的各种事件

9.碎片（Fragment）是一种可以嵌入活动当中的ui片段，它能让程序更加合理和充分的利用大屏幕的空间
