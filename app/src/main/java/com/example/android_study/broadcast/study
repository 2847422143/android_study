1.广播主要分为2种（跨进程通信的一种方式）
    标准广播（NormalBoradcasts）
        完全异步，广播发出之后，所有的广播接收器几乎都会在同一时刻接收到
        效率高，同时也意味着无法被截断
    有序广播
        同步执行，在广播发出去之后，同一时间只有一个广播接收器能够收到这条广播信息，等这个广播接收器的逻辑执行完之后，才会继续传递
        优先级高的广播接收器先收到广播，并且可以截断广播传递

2.本地广播
    使用这个发出的广播只能在应用程序内部进行传递，并且广播接收器也只能接收来自本应用程序发出的广播